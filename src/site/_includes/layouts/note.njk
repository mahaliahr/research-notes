{% extends "layouts/base.njk" %}

{% block content %}
<article class="note">
  <h1>{{ title | formatDailyTitle | default(page.fileSlug) }}</h1>
  
  {{ content | link | dgMedia | safe }}
</article>
{% endblock %}
