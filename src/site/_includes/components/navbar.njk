{%if settings.dgHomeLink === true%}
    <nav class="navbar">
        <div class="navbar-inner">
            <a href="/" style="text-decoration: none;">
                <h1 id="nav-title" style="margin: 15px !important;">{{meta.title or "PhD-Live"}}</h1>
            </a>
        </div>
        <div class="nav-right">
            {% if settings.dgEnableSearch === true %}
              {% include "components/searchButton.njk" %}
            {% endif %}
        </div>
    </nav>
{% else %}
    <div class="empty-navbar" >
      <div id="site-brand" aria-hidden="true" style="display:none;"></div>
      {% if settings.dgEnableSearch === true%}
        {% include "components/searchButton.njk" %}
      {%endif%}
    </div>
{%endif%}