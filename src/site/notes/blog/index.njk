---
layout: "index.njk"
permalink: "/notes/blog/"
title: "Posts"
dgShowSidebar: false
---

{# note: styling is weird for this page so had to hack fixes #}
<style>
  /* Force site bg + graph colors only on /notes/blog/ */
  html, body, .page, .content { background: #f7f7f7 !important; }
  .navbar { background: #f7f7f7 !important; color: #0b0b0b !important; }
  :root {
    --graph-main: #8b6cef;
    --graph-muted: rgba(139,108,239,.45); }

    /* Posts list styling */
  .posts-list {
    list-style: none; padding: 0; margin: 0;
    display: flex; flex-direction: column; gap: 2rem;
  }
  .posts-list > li { border-bottom: 1px solid rgba(0,0,0,0.06); padding-bottom: 2rem; }
  .posts-list > li:last-child { border-bottom: none; padding-bottom: 0; }
  .post-preview h2 { margin: 0 0 .5rem; font-size: 1.5rem; }
  .post-preview h2 a { text-decoration: none; color: #0b0b0b; transition: color .2s; }
  .post-preview h2 a:hover { color: #8b6cef; }
  .post-preview time { display:block; font-size:.9rem; color:#6b7280; margin-bottom:.75rem; }
  .post-preview p { margin:0; color:#6b7280; line-height:1.6; }
</style>

{% if collections.posts.length %}
  <ul class="posts-list">
    {% for post in collections.posts %}
      <li>
        <article class="post-preview">
          <h2><a href="{{ post.url }}">{{ post.data.title | formatDailyTitle | default(post.fileSlug) }}</a></h2>
          {% if post.data.date %}
            <time datetime="{{ post.data.date | isoDate }}">{{ post.data.date | date }}</time>
          {% endif %}
          {% if post.data.description %}
            <p>{{ post.data.description }}</p>
          {% endif %}
        </article>
      </li>
    {% endfor %}
  </ul>
{% else %}
  <p>No posts yet.</p>
{% endif %}